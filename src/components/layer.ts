import { styled, Box } from '@mui/material';

export default styled(Box)({
  position: 'absolute',
  top: '0',
  right: '0',
  bottom: '0',
  left: '0',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
});
